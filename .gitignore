# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/

# Vite cache
.vite/
*.timestamp-*.mjs

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Environment variables
.env
.env.local
.env.*.local

# Test coverage
coverage/

# Misc
*.local

# Videos downloaded by the download centre (user content, often very large).
# The directory carries its own .tasks.json manifest; neither belongs in git.
downloads/

# Local data blobs (the offline ECDICT dictionary DB, ~1 GB, plus its zip).
# Downloaded, not source — never committed.
data/

# Local scratch files (probe output, captured build logs — never committed)
.*.txt
.preview.txt

# Stray tsc build artifacts (vite.config.ts is the real config)
vite.config.js
vite.config.d.ts
*.tsbuildinfo
